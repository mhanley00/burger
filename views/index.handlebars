<div class="container">
    <div class="not-devoured">
                <h3>Ready to Eat🍔</h3>
                {{#each burgers}} {{#unless devoured}}
                <span>{{this.burger_name}}</span>
                <form action="/burger/{{this.id}}" method="POST">
                    <input type="hidden" type="text" value={{this.id}}>
                    <button data-id="{{this.id}}" class="eat-burger" type="submit">Devour It😋</button>
                    </input>
                </form>
                {{/unless}} {{/each}}
                <br>
            
        </div> {{! end of row1}}
        <div class="devoured"> {{!-- right div --}}
            <h3>Devoured</h3>
            {{#each burgers}} {{#if devoured}}
            <span>{{this.burger_name}}</span> {{/if}} {{/each}}
        </div>
    </div> {{!-- end of left container --}}

    <h3>Create Your Perfect Burger🍔</h3>
    <form action="/burger/create" method="POST">
        <input type="text" value={{this.id}} name="new-burger">
        <br>
        <input type="radio" data-dev="false" name="devoured" value="false" checked> Not Devoured
        <br>
        <input type="radio" data-dev="true" name="devoured" value="true"> Devoured
        <br>
        <button data-id="{{this.id}}" class="add-burger" type="submit">Create😋</button>
        </input>
    </form>


